//carousel
var instance = M.Carousel.init({
    fullWidth: true,
    indicators: true,
    height: 480
});

// Or with jQuery

$('.carousel.carousel-slider').carousel({
    fullWidth: true,
    indicators: true
});